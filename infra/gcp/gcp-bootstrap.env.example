# コピー先: ~/.config/cursor/gcp-bootstrap.env
# gcloud で Secret Manager を使うためのプロジェクトとシークレット名（値は非機密）

GCP_PROJECT=lll-fish
GSM_SECRET_NAME=cursor-secret

# 任意: load-secrets-gcp.sh の出力先（既定 ~/.cursor/secrets/secret.env）
# SECRETS_OUT=/home/lll/.cursor/secrets/secret.env
